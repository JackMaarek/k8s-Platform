apiVersion: networking.istio.io/v1beta1
kind: VirtualService
metadata:
  name: {{ .Name }}
  namespace: {{ .Namespace }}
spec:
  hosts:
    - {{ .Name }}
  http:
    - route:
        - destination:
            host: {{ .Name }}
            port:
              number: 80
