# lpcdm — LOCAL-DEV fixture
# Used to validate the infra: Istio, ServiceMonitor, multi-env, ArgoCD sync.
# NOT part of the template. Generated by platform-bot in a real scenario.
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: lpcdm
  namespace: argocd
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  project: applications

  source:
    repoURL: https://github.com/PodYourLife/k8s-platform.git
    targetRevision: local-test
    path: kubernetes/helm/sample-app
    helm:
      valueFiles:
        - ../../helm/values/lpcdm.yaml

  destination:
    server: https://kubernetes.default.svc
    namespace: development

  syncPolicy:
    automated:
      prune: true
      selfHeal: true
      allowEmpty: false
    syncOptions:
      - CreateNamespace=false
      - PrunePropagationPolicy=foreground
      - PruneLast=true
    retry:
      limit: 5
      backoff:
        duration: 5s
        factor: 2
        maxDuration: 3m

  revisionHistoryLimit: 10

  ignoreDifferences:
    - group: apps
      kind: Deployment
      jsonPointers:
        - /spec/replicas
