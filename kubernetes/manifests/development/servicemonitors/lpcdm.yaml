# lpcdm ServiceMonitor — LOCAL-DEV fixture
# Generated by platform-bot app add --monitor in a real scenario.
apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: lpcdm
  namespace: development
  labels:
    app.kubernetes.io/name: lpcdm
    release: prometheus
spec:
  selector:
    matchLabels:
      app.kubernetes.io/name: lpcdm
  endpoints:
    - port: http
      interval: 30s
      path: /metrics
  namespaceSelector:
    matchNames:
      - development
